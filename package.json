{
  "name": "personal-portfolio",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently -n frontend,backend -c cyan,green \"npm run dev --workspace frontend\" \"npm run dev --workspace backend\"",
    "build": "npm run build --workspace backend && npm run build --workspace frontend",
    "lint": "npm run lint --workspace frontend",
    "start": "concurrently -n frontend,backend -c cyan,green \"npm run preview --workspace frontend -- --host 0.0.0.0\" \"npm run start --workspace backend\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
