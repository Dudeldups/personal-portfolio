name: your-portfolio

services:
  app:
    build:
      context: .
      dockerfile: ./backend/Dockerfile
    container_name: your-portfolio
    restart: unless-stopped
    ports:
      - "4000:4000"
    env_file:
      - ./backend/.env
